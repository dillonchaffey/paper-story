const Header = () => {
  const template = `
    <header>
      <h1>Paper Story</h1>
      <p>An example web app structure based off of 
        <a href="https://github.com/bradtraversy/vanilla-parcel-boilerplate">Vanilla Parcel Boilerplate</a> 
      </p> 
    </header>
  `;

  return template;
};

export default Header;
